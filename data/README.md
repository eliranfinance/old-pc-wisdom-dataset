# Data files

- `old_pc_wisdom.jsonl` — 16 newline-delimited JSON records
- `schema.json` — JSON Schema for one record

The JSONL file is intentionally small and readable. It contains synthesized principles, not copied source documents.
